import { sharedDisplayTests } from './sharedDisplayTests';
import { DisplayArrayReference } from '../../../../Display';

describe('DisplayArrayReference', () => {
  sharedDisplayTests(DisplayArrayReference);
});
