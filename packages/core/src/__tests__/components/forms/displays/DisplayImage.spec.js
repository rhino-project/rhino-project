import { sharedDisplayTests } from './sharedDisplayTests';
import { DisplayImage } from '../../../../components/forms/displays/DisplayImage';

describe('DisplayImage', () => {
  sharedDisplayTests(DisplayImage);
});
