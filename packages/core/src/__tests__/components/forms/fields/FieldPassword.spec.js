import FieldPassword from 'rhino/components/forms/fields/FieldPassword';
import { sharedFieldTests } from './sharedFieldTests';

describe('FieldPassword', () => {
  sharedFieldTests(FieldPassword);
});
