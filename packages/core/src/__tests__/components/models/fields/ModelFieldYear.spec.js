import { sharedFieldTests } from './sharedFieldTests';
import ModelFieldYear from 'rhino/components/models/fields/ModelFieldYear';

describe('ModelFieldYear', () => {
  sharedFieldTests(ModelFieldYear);
});
