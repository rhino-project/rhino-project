import { sharedFieldTests } from './sharedFieldTests';
import ModelFieldDate from 'rhino/components/models/fields/ModelFieldDate';

describe('ModelFieldDate', () => {
  sharedFieldTests(ModelFieldDate);
});
