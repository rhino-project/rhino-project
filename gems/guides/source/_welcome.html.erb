<h2>Rhino Guides (<%= @edge ? @edge[0, 7] : @version %>)</h2>

<% if @edge %>
<p>
  These are <b>Edge Guides</b>, based on <a href="https://github.com/nubinary/boilerplate_server/tree/<%= @edge %>">main@<%= @edge[0, 7] %></a>.
</p>
<% else %>
<p>
  These are the new guides for Rhino 2.0 based on <a href="https://github.com/nubinary/boilerplate_server/tree/<%= @version %>"><%= @version %></a>.
  These guides are designed to make you immediately productive with Rhino, and to help you understand how all of the pieces fit together.
</p>
<% end %>

